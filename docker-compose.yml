services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: assetlane-mailpit
    ports:
      - "1025:1025"   # SMTP — nodemailer sends here
      - "8025:8025"   # web UI — http://localhost:8025
    restart: unless-stopped
